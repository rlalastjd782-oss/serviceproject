# Changelog

## v2.5.7 - 2026-05-29

- 요청 단위 계정/설정 캐시를 추가하고 계정 `last_seen_at` 갱신을 5분 단위로 제한해 매 요청 DB write를 줄였습니다.
- SQLite 성능 진단 서비스, `ANALYZE` 실행, 대표 `EXPLAIN QUERY PLAN` 표시, 핵심 인덱스 누락 점검을 QA 화면에 추가했습니다.
- 기록/식단/PR 분석용 복합 인덱스를 보강했습니다.
- PythonAnywhere 정적 파일 배포 점검과 소스 길이 진단을 QA 화면에 추가했습니다.
- 서비스워커 사전 캐시에서 HTML 페이지를 제외하고 핵심 정적 파일 중심으로 경량화했습니다.
- 검증: `ruff check health_tracker tests`, `python -m unittest discover -v` 30개, `python -m compileall health_tracker tests`, 주요 JS `node --check`, `git diff --check`.

## v2.5.6 - 2026-05-28

- 매 요청마다 실행되던 앱 DB/계정 DB 초기화를 DB 파일 경로별 1회 초기화 캐시로 변경했습니다.
- 정적 파일, 서비스워커, favicon GET 요청은 인증/DB 조회 흐름을 건너뛰도록 해 배포 환경의 불필요한 SQLite 접근을 줄였습니다.
- `/sw.js`, `/favicon.ico`, `/static/css/styles.css`가 DB 쿼리 없이 응답하는 회귀 검증을 추가했습니다.
- 앱/manifest/service worker 버전을 `2.5.6`으로 갱신했습니다.
- 검증: `ruff check health_tracker tests`, `python -m unittest discover -v` 28개, `python -m compileall health_tracker tests`, 주요 JS `node --check`.

## v2.5.5 - 2026-05-28

- 브라우저가 자동 요청하는 루트 `/favicon.ico` 경로를 추가해 PythonAnywhere 배포 콘솔의 favicon 404를 제거했습니다.
- 기존 `static/icon.svg`를 favicon 응답과 레이아웃 icon 링크에 재사용했습니다.
- 인증 전에도 favicon을 받을 수 있도록 공개 GET/공유 엔드포인트 허용 목록에 `root_favicon`을 추가했습니다.
- 서비스워커 precache와 앱/manifest 버전을 `2.5.5`로 갱신했습니다.
- 검증: `ruff check health_tracker tests`, `python -m unittest discover -v` 28개, `python -m compileall health_tracker tests`, 주요 JS `node --check`.

## v2.5.4 - 2026-05-28

- 모바일 폭에서 공통 버튼 규칙이 상단 탭 `.tab-btn`까지 `width: 100%`로 잡아 `오늘` 탭만 보이던 문제를 수정했습니다.
- 상단 메뉴 탭은 `.tabs .tab-btn` 예외로 분리해 `오늘/기록/분석` 등이 가로 메뉴로 표시되게 했습니다.
- CSS 캐시 반영을 위해 앱/manifest/service worker 버전을 `2.5.4`로 갱신했습니다.
- 회귀 방지를 위해 `ui_rebuild.css`가 모바일 공통 버튼 규칙에 `.tab-btn`을 다시 포함하지 않는지 검증하는 테스트를 추가했습니다.
- 검증: `ruff check health_tracker tests`, `python -m unittest discover -v` 28개, `python -m compileall health_tracker tests`, 주요 JS `node --check`.

## v2.5.3-hotfix - 2026-05-28

- static 폴더 분리 후 실행 중인 서버가 예전 템플릿을 응답하면 CSS가 깨질 수 있음을 확인하고 로컬 서버를 재시작했습니다.
- 로그인/앱/기록/식단/더보기 렌더 HTML에 예전 `/static/*.css` 및 `/static/*.js` 경로가 남지 않는 회귀 테스트를 추가했습니다.
- 검증: `ruff check health_tracker tests`, `python -m unittest discover -v` 28개, `python -m compileall health_tracker tests`, 주요 JS `node --check`, `git diff --check`.

## v2.5.3 - 2026-05-28

- `static` 루트에 몰려 있던 CSS와 JS를 `static/css`, `static/js` 폴더로 분리했습니다.
- 사용자/관리자/인증 레이아웃의 정적 파일 로드 경로를 새 폴더 구조에 맞게 갱신했습니다.
- 서비스워커 precache 목록을 새 정적 파일 경로로 갱신하고 캐시 버전을 `2.5.3`으로 올렸습니다.
- 정적 파일 무결성 테스트와 서비스워커 자산 검증 테스트를 새 경로 기준으로 수정했습니다.
- 검증: `ruff check health_tracker tests`, `python -m unittest discover -v`, `python -m compileall health_tracker tests`, 주요 JS `node --check`, `git diff --check`.

## v2.5.2 - 2026-05-28

- 오늘 운동 탭에 운동 중 집중 모드를 추가했습니다.
- `focus=1` 상태에서는 장소 관리, 추천/회복/보조 도구 같은 보조 섹션을 숨기고 타이머, 빠른 이동, 오늘 할 일, 운동 입력, 오늘 운동, 완료 리뷰 중심으로 화면을 정리합니다.
- 집중 모드 빠른 이동에 완료 리뷰 앵커를 추가했습니다.
- 모바일 운동 중 빠른 메뉴가 5개 항목에서도 고정 폭으로 유지되도록 스타일을 보강했습니다.
- 검증: `ruff check health_tracker tests`, `python -m unittest discover -v`, `python -m compileall health_tracker tests`, 주요 JS `node --check`, `git diff --check`.

## v2.5.1 - 2026-05-28

- 개발 QC 도구로 `ruff`를 설치하고 `requirements-dev.txt`에 고정했습니다.
- `pyproject.toml`에 Ruff 설정을 추가했습니다. 현재 라우트 모듈은 `globals().update(ctx)` 기반 의존성 주입 구조라 라우트 파일의 F821은 예외 처리했습니다.
- 테스트 파일의 미사용 보안 헬퍼 import를 제거했습니다.
- 검증: `ruff check health_tracker tests`, `python -m unittest discover -v`, `python -m compileall health_tracker tests`, 주요 JS `node --check`, `git diff --check`.

## v2.5.0 - 2026-05-28

- 오늘 운동 완료 영역에 운동 완료 리뷰 카드를 추가했습니다. 완료/진행 상태, 총 세트, 볼륨, PR, 유산소, 오늘 피드백, 다음 행동을 한 번에 볼 수 있습니다.
- 운동별 목표 진행률을 추가했습니다. 기존 운동 설정의 목표 kg/회/세트와 실제 최고 기록을 비교해 오늘 운동 카드 안에 진행률 막대로 표시합니다.
- 완료 리뷰와 목표 진행률은 기존 세션/세트/PR/운동 설정 데이터를 재사용해 별도 DB 마이그레이션 없이 동작합니다.
- 모바일 화면에서 완료 리뷰 카드가 1열로 정리되도록 반응형 스타일을 보강했습니다.
- PWA manifest와 service worker 캐시 버전을 `2.5.0`으로 갱신했습니다.
- 검증: `python -m unittest discover -v`, `python -m compileall health_tracker tests`, 주요 JS `node --check`, `git diff --check`.

## v2.4.5 - 2026-05-28

- 분석, 캘린더, 식단 주/월간, 기록 검색 라우트를 별도 모듈로 분리해 `routes/main.py`를 등록 허브 중심으로 정리했습니다.
- 홈, 프로그램 적용, 최근 세션 API 라우트를 `routes/home.py`, `routes/programs.py`, `routes/api.py`로 분리했습니다.
- 운동 세션 생성/조회, 운동 목록, 최근 세트, 운동 통계, 즐겨찾기 운동, 과부하 제안 SQL을 서비스 계층으로 옮겨 `app.py`의 직접 DB 책임을 줄였습니다.
- 기본 브랜치 변경 이후 앱 갱신시각이 `master`를 참조하던 문제를 `main` 기준으로 수정했습니다.
- PWA manifest와 service worker 캐시 버전을 `2.4.5`로 갱신했습니다.
- 검증: `python -m unittest discover -v`, `python -m compileall health_tracker tests`, 주요 JS `node --check`, `git diff --check`.

## v2.4.4 - 2026-05-28

- 식단 빠른선택과 장소 인사이트 SQL을 서비스 모듈로 분리해 `app.py` 책임을 줄였습니다.
- 장소 인사이트는 장소별 반복 조회 대신 bulk 집계 방식으로 변경해 N+1 쿼리를 줄였습니다.
- SQLite 연결에 `foreign_keys`, `busy_timeout`, `journal_mode=WAL`, `synchronous=NORMAL` 설정을 추가하고 분석/검색용 복합 인덱스를 보강했습니다.
- 요청 처리 시간, DB 쿼리 수, `Server-Timing` 응답 헤더를 추가해 성능 계측 기반을 만들었습니다.
- 알림 스크립트, 식단 입력 스크립트, 식단 CSS, 오늘 화면 partial, 식단 입력 partial, 보안/마이그레이션 테스트를 분리했습니다.
- 로컬 `__pycache__` 산출물을 정리했습니다.
- 검증: `python -m unittest discover -v` 27개 통과, `python -m compileall health_tracker tests`, 주요 JS `node --check`, `git diff --check`.

## v2.4.3 - 2026-05-28

- 식단 저장 시 입력한 모든 음식이 자동으로 즐겨찾기에 등록되던 흐름을 제거했습니다.
- 식단 입력 빠른 선택을 `고정 음식`, `자주 먹는 음식`, `최근 입력 음식`으로 분리했습니다.
- 고정 음식은 수동 등록한 항목만 최대 6개 노출하고, 화면에서 바로 고정 해제할 수 있게 했습니다.
- 최근 입력 음식은 식사 구분별 최근 6개만 표시해 모바일 식단 입력 화면이 길어지지 않도록 정리했습니다.
- PWA manifest와 service worker 캐시 버전을 `2.4.3`으로 갱신했습니다.
- 검증: `python -m unittest discover -v`, `python -m compileall health_tracker tests`, `node --check static/app.js`, `node --check static/ui_interactions.js`, `node --check static/workout_tools.js`, `node --check static/offline_queue.js`, `node --check static/workout_entry.js`, `git diff --check`.

## v2.4.2 - 2026-05-28

- 장소 인사이트에서 등록 장비와 실제 사용 장비를 비교해 장비활용률과 미사용 장비를 표시하도록 확장했습니다.
- 등록 장비는 장비명뿐 아니라 장비 유형까지 기준으로 삼아 운동 입력에서 선택한 분류와 더 정확히 맞물리도록 보강했습니다.
- 미사용 장비 행은 별도 강조 스타일을 적용해 장소별로 아직 기록이 없는 장비를 빠르게 확인할 수 있게 했습니다.
- PWA manifest와 service worker 캐시 버전을 `2.4.2`로 갱신했습니다.
- 검증: `python -m unittest discover -v`, `python -m compileall health_tracker tests`, `node --check static/app.js`, `node --check static/ui_interactions.js`, `node --check static/workout_tools.js`, `node --check static/offline_queue.js`, `node --check static/workout_entry.js`, `git diff --check`.

## v2.4.1 - 2026-05-28

- 기본 브랜치를 `main`으로 전환한 뒤 코드 구조 고도화를 진행했습니다.
- `routes/main.py`에 몰려 있던 오늘 입력, 식단/세트 수정, 데이터 입출력 라우트를 `today_actions.py`, `entries.py`, `data.py`로 분리했습니다.
- 계정/관리자 헬퍼를 `health_tracker/app_accounts.py`로 분리해 `app.py`의 인증/계정 책임을 줄였습니다.
- `styles.css`를 기본 스타일, 화면별 스타일, 반응형 스타일로 나누고 `feature_pages.css`, `responsive.css`를 추가했습니다.
- 오프라인 폼 저장 큐를 `offline_queue.js`로 분리했습니다.
- 모바일 리스트, 카드, 필터, 입력 행의 줄바꿈과 터치 영역을 보강했습니다.
- 검증: `python -m unittest discover -v`, `python -m compileall health_tracker tests`, `node --check static/app.js`, `node --check static/offline_queue.js`, `node --check static/workout_entry.js`.

## v2.4.0 - 2026-05-28

- 인증 라우트를 `health_tracker/routes/auth.py`로 분리해 로그인, 회원가입, 로그아웃 흐름을 메인 라우트에서 분리했습니다.
- 설정 라우트를 `health_tracker/routes/settings.py`로 분리해 설정 잠금, 계정 관리, 앱 기본값, QA 더미 데이터, 빈 세션 정리 기능을 독립 관리하도록 정리했습니다.
- 공통 반응형 CSS를 보강해 카드, 리스트, 필터, 버튼, 테이블, 폼이 모바일/폴드 폭에서 겹치거나 밀리지 않도록 안전 규칙을 추가했습니다.
- 오늘 화면의 루틴/운동 입력, 운동 기록, 식단 기록 영역을 partial 템플릿으로 분리했습니다.
- 분석 화면의 운동별 분석과 장비별 분석 영역을 partial 템플릿으로 분리해 `summary.html` 크기를 줄였습니다.
- PWA manifest와 service worker 캐시 버전을 `2.4.0`으로 올려 배포 후 정적 파일 갱신이 더 명확하게 반영되도록 했습니다.
- 검증: `python -m unittest discover -v`, `python -m compileall health_tracker tests`, `node --check static/app.js`, `node --check static/timers.js`, `node --check static/workout_entry.js`.

## v2.3.11 - 2026-05-28

- 오늘 운동 리스트를 템플릿 내부 DB 호출 대신 `today_context`에서 미리 만든 `workout_groups`로 렌더링하도록 바꿨습니다.
- 운동 탭 렌더링 중 템플릿이 직접 `grouped_sets_for_session()`을 호출하던 병목 후보를 제거했습니다.
- 남은 큰 분리 대상은 `today/index.html`, `summaries/summary.html`, `routes/main.py`로 확인했습니다.
- 검증: `python -m unittest discover -v`, `python -m compileall health_tracker tests`, `node --check static/app.js`, `node --check static/workout_entry.js`.

## v2.3.10 - 2026-05-28

- 오늘 화면 컨텍스트를 모드별로 나누어 운동 탭 진입 시 식단/전체 요약용 데이터를 불필요하게 만들지 않도록 정리했습니다.
- 운동 탭에서 CSS로만 숨겨지던 전체 요약/식단 영역을 Jinja 조건 렌더링으로 전환해 HTML 렌더링 비용을 줄였습니다.
- 운동명 수정 영역이 빠지는 회귀를 테스트로 확인하고 조건 범위를 다시 조정했습니다.
- 검증: `python -m unittest discover -v`, `python -m compileall health_tracker tests`.

## v2.3.9 - 2026-05-28

- 모바일 운동 화면에서 빠른 메뉴 sticky 위치가 상단 탭과 겹치지 않도록 조정했습니다.
- 빠른 메뉴 앵커 이동 시 오늘 운동, 입력, 휴식 타이머 섹션이 상단 메뉴 아래에 가려지지 않도록 `scroll-margin-top`을 추가했습니다.
- 모바일 운동 메뉴 겹침 방지 CSS 회귀 테스트를 추가했습니다.

## v2.3.8 - 2026-05-28

- 모바일 헤더에서 앱 이름, 환영 문구, 버전 배지가 겹치지 않도록 430px 이하 화면에서는 헤더를 2줄 구조로 변경했습니다.
- 모바일 헤더 높이에 맞춰 상단 메뉴 sticky 위치를 조정했습니다.
- 헤더 계정/버전 영역 렌더링 회귀 테스트를 추가했습니다.

## v2.3.7 - 2026-05-28

- 관리자 대시보드에 사용자 검색, 상태 필터, 정렬 기능을 추가했습니다.
- 조치 필요 사용자 섹션을 추가해 미사용, 기록 부족, 비활성, DB 확인 필요 계정을 바로 볼 수 있게 했습니다.
- 관리자 활동 로그 테이블과 화면을 추가하고 비밀번호 초기화, 상태 변경, 메모 수정, 데이터 내보내기를 기록합니다.
- 사용자 상세에서 데이터 내보내기 버튼을 제공하고 JSON에 원본 테이블과 사용 요약을 함께 담도록 했습니다.
- 관리자 대시보드/상세 템플릿의 한글 문구와 모바일 정렬을 정리했습니다.

## v2.3.6 - 2026-05-28

- 관리자 대시보드의 사용자 현황과 운동/식단 집계에서 관리자 계정을 제외했습니다.
- 관리자 계정 상세 접근은 사용자 상세 대상이 아니므로 대시보드로 되돌리도록 막았습니다.
- 관리자 대시보드에 가입/활성, 기록 상태, 데이터 규모를 확인하는 운영 체크포인트를 추가했습니다.
- 관리자 현황 회귀 테스트에 관리자 계정 제외와 사용자 상세 접근 제한을 추가했습니다.

## v2.3.5 - 2026-05-28

- 가입 전 미리보기 화면을 히어로, 모바일 샘플 카드, 핵심 기능 요약, 기록/분석/장소/식단 카드 구성으로 재정리했습니다.
- 깨진 한글 문구를 정상 한글로 교체하고 샘플 데이터가 실제 앱 기능 흐름을 보여주도록 바꿨습니다.
- 모바일 화면에서 미리보기 카드와 기록 행이 한 줄로 눌리지 않도록 반응형 레이아웃을 보강했습니다.
- 미리보기 회귀 테스트 기준을 새 화면 문구와 장소 샘플까지 확인하도록 업데이트했습니다.

## v2.3.4 - 2026-05-28

- 가입 전 공개 미리보기 페이지 `/auth/preview`를 추가했습니다.
- 로그인/회원가입 화면에서 미리보기로 이동할 수 있게 연결했습니다.
- 미리보기 화면은 샘플 UI만 보여주며, 실제 DB 데이터와 입력 폼은 노출하지 않도록 구성했습니다.
- 비로그인 상태에서도 미리보기만 접근 가능하도록 공개 GET 엔드포인트를 제한적으로 추가했습니다.
- 미리보기 공개 범위와 샘플 전용 렌더링을 회귀 테스트로 고정했습니다.

## v2.3.3 - 2026-05-28

- 헤더에서 계정 환영 문구와 앱 버전을 분리해 현재 로그인 계정과 버전을 명확히 표시했습니다.
- 로그인 화면의 사용자/관리자 선택 탭 대비를 강화했습니다.
- 사용자 회원가입을 `/auth/signup` 전용 화면으로 분리하고 로그인 화면에는 회원가입 버튼만 남겼습니다.
- 사용자/관리자 로그인 화면에 비밀번호 분실 시 문의 안내 문구를 추가했습니다.
- 관리자 대시보드에서 관리자 본인 비밀번호를 변경할 수 있게 했고, 설정 비밀번호 해시도 함께 동기화되도록 했습니다.

## v2.3.2 - 2026-05-28

- 인증 전용 로그인 화면의 폼에 CSRF hidden input을 직접 렌더링하도록 수정했습니다.
- `layouts/auth.html`이 앱 공통 JS를 로드하지 않아 브라우저 로그인 POST가 400 Bad Request로 막히던 문제를 해결했습니다.
- 로그인 화면 CSRF 필드 회귀 테스트를 추가했습니다.

## v2.3.1 - 2026-05-28

- `/`를 화면 렌더링이 아닌 역할별 진입 라우터로 변경했습니다.
- 기존 오늘 운동 화면을 `/app`으로 분리하고, 비로그인 사용자는 `/auth/login`으로만 이동하게 했습니다.
- 로그인 화면을 `auth/login.html`과 `layouts/auth.html`로 분리해 앱 메뉴가 보이지 않도록 정리했습니다.
- 관리자 화면은 `layouts/admin.html`을 사용하도록 분리해 사용자 앱 메뉴와 섞이지 않게 했습니다.
- PWA 시작 URL을 `/`로 돌려 세션 상태에 따라 로그인/사용자 앱/관리자 콘솔로 이동하게 했습니다.
- 비로그인 `/` 접근 차단과 `/app` 사용자 홈 흐름을 테스트로 고정했습니다.

## v2.3.0 - 2026-05-28

- 앱 첫 진입을 로그인 화면으로 고정하고, 비로그인 사용자의 운동/기록/분석/식단/더보기 접근을 차단했습니다.
- 관리자 로그인 후에는 `/admin` 관리자 화면만 볼 수 있도록 서버 라우트와 메뉴를 분리했습니다.
- 사용자 로그인 후에는 기존 운동 앱 화면만 사용할 수 있고, 관리자 페이지 직접 접근은 관리자 로그인으로 돌리도록 정리했습니다.
- 회원가입에 비밀번호 확인 입력을 추가하고, 첫 일반 사용자 계정이 기존 메인 운동 DB를 이어받도록 계정 DB 매핑을 보정했습니다.
- 로그인 권한 흐름과 계정별 데이터 분리 테스트를 새 정책에 맞게 갱신했습니다.

## v2.2.0 - 2026-05-28

- 로그인 페이지를 관리자/사용자 탭으로 재구성하고 사용자 탭에 아이디/비밀번호 회원가입을 추가했습니다.
- 관리자 전용 `/admin` 대시보드와 `/admin/users/<id>` 사용자 상세 현황 페이지를 추가했습니다.
- 관리자에서 사용자 비밀번호 초기화, 비활성/활성 전환, 표시 이름/메모 관리를 할 수 있게 했습니다.
- 설정 화면의 계정 운영 진입점을 관리자 대시보드로 정리하고, 계정별 데이터 분리/권한 회귀 테스트를 보강했습니다.

## v2.1.0 - 2026-05-27

- 2인 사용을 위한 계정 로그인/로그아웃과 설정 내 계정 생성 기능을 추가했습니다.
- 기본 `admin` 계정은 기존 데이터베이스를 그대로 사용하고, 추가 계정은 계정별 별도 DB 파일을 사용해 운동/식단/분석/장소 데이터가 섞이지 않도록 했습니다.
- 헤더에 현재 계정 표시를 추가하고, 두 계정의 기록이 서로 보이지 않는 회귀 테스트를 추가했습니다.

## v2.0.7 - 2026-05-27

- PWA head에 표준 `mobile-web-app-capable` 메타 태그를 추가해 Chrome 계열 경고를 해소했습니다.
- iOS 홈 화면 호환성을 위해 기존 `apple-mobile-web-app-capable` 태그는 유지했습니다.

## v2.0.6 - 2026-05-27

- 장비 카테고리의 `머신`을 `핀머신`, `플레이트로디드머신`으로 분리했습니다.
- 기존 `머신` 저장 데이터는 앱 초기화 시 `핀머신`으로 정리하고, 스미스/플레이트 로디드 계열 입력은 `플레이트로디드머신`으로 저장되도록 보강했습니다.
- 장비 선택 select의 글자 크기와 간격을 줄여 긴 카테고리명이 칸 안에서 깨지지 않도록 조정했습니다.

## v2.0.5 - 2026-05-26

- 운동 완료 후 페이지가 다시 로드되면서 저장된 운동시간이 타이머 표시로 복원되던 문제를 수정했습니다.
- 완료 시 로컬 타이머 상태에 `completedReset` 플래그를 저장해 저장된 운동시간은 유지하되 진행 타이머는 `00:00:00`으로 유지합니다.
- 배포 서버 HTML이 오래된 경우에도 `/sessions/<id>/complete` action form을 감지해 타이머 리셋이 동작하도록 보강했습니다.

## v2.0.4 - 2026-05-26

- 운동 완료 버튼을 누를 때 저장된 운동시간은 유지하고 화면 타이머만 `00:00:00`으로 리셋되도록 했습니다.
- 완료 form에 `data-workout-complete-form`을 추가하고, submit 시 로컬 타이머 상태만 초기화하는 `resetWorkoutClockDisplayOnly`를 연결했습니다.
- 운동 완료 후 서버 저장 API를 추가 호출하지 않도록 해 기존 저장 시간 기록은 그대로 보존합니다.

## v2.0.3 - 2026-05-26

- 오늘 식단 카드가 공통 기록 카드 2열 selector에 계속 덮이던 문제를 수정했습니다.
- 공통 기록 카드 selector에서 `meal-record-card`를 제외하고, 파일 하단에 더 강한 식단 카드 1열 고정 규칙을 추가했습니다.
- CSS 회귀 테스트에 식단 카드 제외 selector와 전용 override 존재 여부를 추가했습니다.

## v2.0.2 - 2026-05-26

- 오늘 식단 리스트 카드가 공통 기록 카드 2열 규칙을 받아 깨지던 문제를 수정했습니다.
- 식단 그룹 카드와 음식 행에 `meal-record-card`, `meal-record-item` 전용 클래스를 추가하고 식단 리스트 전용 grid 레이아웃을 적용했습니다.
- 식단 리스트 렌더링 회귀 마커와 CSS 무결성 검증을 추가 확인했습니다.

## v2.0.1 - 2026-05-26

- 기록 탭 부위별 분석 카드에서 날짜와 운동시간/칼로리 텍스트가 같은 grid 영역에 겹치던 문제를 수정했습니다.
- 부위별 분석 카드의 날짜, 상세 지표, PR 줄을 각각 별도 grid 영역으로 분리했습니다.
- 부위별 분석 렌더링 회귀 마커와 CSS 무결성 검증을 추가 확인했습니다.

## v2.0.0 - 2026-05-26

- 2.0 릴리즈 버전으로 전환하고 PWA manifest/service worker 캐시를 `2.0.0` 기준으로 갱신했습니다.
- 오늘 운동/식단 입력, 기록/분석, 위치/장비, 설정 잠금, 데이터 백업/복원, 연간 QA 흐름을 2.0 기준 기능 범위로 정리했습니다.
- `app.py`의 설정, 데이터 정리/복원, 요약 조회, 연간 export, 최근 세션 SQL을 서비스 계층으로 분리해 릴리즈 전 구조 정리를 마무리했습니다.
- README 기능 목록을 현재 앱 기능 기준으로 갱신했습니다.

## v1.25.17 - 2026-05-26

- 최근 운동 세션 API 조회 SQL을 `services/workout.py`로 이동했습니다.
- `/api/sessions`가 쓰는 `app.py` 함수는 wrapper만 남겨 workout 데이터 조회 책임을 서비스 계층으로 정리했습니다.
- 최근 세션 API, 오늘 운동 렌더링, 메인/운동·식단 회귀 테스트를 통과했습니다.

## v1.25.16 - 2026-05-26

- 연간 JSON/CSV export 조립 로직을 `services/yearly.py`로 이동했습니다.
- `app.py`의 연간 export 함수는 기존 라우트 호환 wrapper로 줄이고, CSV 직렬화와 payload 조립은 yearly 서비스에서 관리하도록 정리했습니다.
- 연간 QA/export 회귀 테스트와 연간 JSON/CSV HTTP 검증을 통과했습니다.

## v1.25.15 - 2026-05-26

- 일별/주별 요약 SQL 조회 로직을 `services/summary.py`로 이동했습니다.
- `app.py`에는 기존 호출부 호환 wrapper만 남겨 분석/기록 데이터 조회 책임을 서비스 계층으로 분리했습니다.
- 일별/주별/월별 분석 렌더링과 분석 메뉴/샘플 데이터 회귀 테스트를 통과했습니다.

## v1.25.14 - 2026-05-26

- 전체 데이터 복원/import 로직을 `services/export.py`의 `import_all_data_to_db`로 이동했습니다.
- export/import가 같은 `EXPORT_TABLES` 순서를 공유하도록 정리해 테이블 목록 중복을 줄였습니다.
- 연간 JSON export 라우트가 `json`을 직접 import하도록 고쳐 `app.py` 전역 import에 의존하지 않게 했습니다.
- 설정/데이터센터 렌더링과 데이터 삭제/설정/메인 회귀 테스트를 통과했습니다.

## v1.25.13 - 2026-05-26

- 데이터 전체 삭제 백업, 빈 운동 세션 삭제, 내부 점검 데이터 삭제 로직을 `services/data_maintenance.py`로 분리했습니다.
- `app.py`의 운영성 데이터 정리 함수는 기존 라우트 호환 wrapper만 남기도록 줄였습니다.
- 샘플 데이터, 위험 삭제 확인, 설정 잠금 회귀 테스트와 설정/데이터센터 렌더링 검증을 통과했습니다.

## v1.25.12 - 2026-05-26

- 설정값 저장과 설정 비밀번호 해시/검증 처리를 `services/settings.py`로 분리했습니다.
- `app.py`에는 Flask 세션 잠금 상태 처리만 남기고 DB 설정 로직은 서비스 계층으로 이동했습니다.
- 설정 잠금/해제/재설정과 방문자 read-only 보안 회귀 테스트를 통과했습니다.

## v1.25.11 - 2026-05-26

- 분석/기록 요약 화면의 상단 내비게이션 조건문을 `summaries/_summary_nav.html` partial로 분리했습니다.
- `summaries/summary.html`의 공통 진입부를 줄여 일별 기록과 분석 탭 내비게이션을 한 곳에서 관리하도록 정리했습니다.
- 일별/주간 분석 렌더링과 메뉴 분리 회귀 테스트를 통과했습니다.

## v1.25.10 - 2026-05-26

- 오늘 식단 기록의 음식 행 읽기/수정/삭제 블록을 `today/_meal_record_item.html` partial로 분리했습니다.
- `today/index.html`의 식단 반복 행 본문을 include로 줄여 운동/식단 기록 행 구조를 같은 방식으로 관리하도록 정리했습니다.
- 식단 모드 렌더링과 운동/식단 회귀 테스트를 통과했습니다.

## v1.25.9 - 2026-05-26

- 오늘 운동 기록의 세트 행 읽기/수정/복사/삭제 블록을 `today/_workout_set_item.html` partial로 분리했습니다.
- `today/index.html`의 반복 세트 행 본문을 include로 줄여 수정 UI와 세트 표시 구조를 한 곳에서 관리하도록 정리했습니다.
- 렌더링 유지 검증과 운동/식단 회귀 테스트를 통과했습니다.

## v1.25.8 - 2026-05-26

- 날짜별 기록 UI를 `summaries/_daily_records.html` partial로 분리해 `summaries/summary.html` 본문을 줄였습니다.
- 오늘 운동 입력의 빠른 운동 선택 패널을 `today/_workout_quick_panel.html` partial로 분리해 `today/index.html`의 과밀도를 낮췄습니다.
- 렌더링 결과를 유지한 상태에서 긴 템플릿 블록을 분리해 2.0 전 구조 정리를 진행했습니다.

## v1.25.7 - 2026-05-26

- QA 리포트의 오래된 `/sw.js` 캐시 기준 문구를 현재 앱 버전 기준으로 표시하도록 정리했습니다.
- `ui_rebuild.css`의 특정 패치 버전 주석을 최종 override 파일 역할에 맞게 일반화했습니다.
- CSS 중괄호 균형과 과거에 문제를 만든 중복 selector 패턴을 검사하는 회귀 테스트를 추가했습니다.
- service worker precache 테스트에 `ui_rebuild.css` 포함 여부를 명시했습니다.

## v1.25.6 - 2026-05-26

- `styles.css`의 깨진 selector 중괄호를 수정해 캘린더, 라이브러리, 설정, 기록 관련 후반부 CSS가 브라우저에서 무시되던 문제를 해결했습니다.
- 월간 캘린더를 7열 카드 그리드로 복구하고, 모바일에서는 날짜별 리스트로 자연스럽게 전환되도록 보정했습니다.
- 운동 라이브러리, 빠른 운동 선택, 날짜별 기록, 부위별 분석, 데이터 관리/QA 카드의 공통 여백, 버튼, 카드 밀도를 다시 맞췄습니다.
- PWA 캐시와 manifest 버전을 `v1.25.6`으로 갱신했습니다.

이 프로젝트는 Semantic Versioning(`MAJOR.MINOR.PATCH`)을 기준으로 관리합니다.

## v1.25.5 - 2026-05-26

- `static/ui_rebuild.css`를 추가해 깨진 카드/목록/운동 입력 UI 보정 규칙이 `rules.css` 뒤에서 마지막으로 적용되게 했습니다.

- 운동 입력, 세트 빌더, 운동 바로 선택 버튼을 공통 레이아웃으로 다시 정리했습니다.
- 최근 기록, 주별/월별 기록, 날짜별 기록의 뱃지형 지표가 한 줄 텍스트로 붙지 않도록 카드형 metric grid로 보정했습니다.
- 분석 리포트의 상세 행과 부위별 분석 카드를 제목/값 영역으로 분리해 긴 문장이 붙어 보이지 않도록 수정했습니다.
- Galaxy Fold 계열 폭에서 세트 행, 복사/삭제 버튼, 장비 선택, 기록 카드가 1열로 무너지지 않도록 900px 이하 반응형 규칙을 추가했습니다.
- CSS/JS 정적 파일 URL에 앱 버전을 붙여 모바일/PWA 캐시가 예전 UI를 계속 보여주는 문제를 줄였습니다.
- PWA 캐시와 앱 버전을 `v1.25.5`로 갱신했습니다.

## v1.25.4 - 2026-05-26

- 운동 바로 선택의 최근/즐겨찾기/루틴 탭과 운동 후보 버튼을 같은 버튼 체계로 맞췄습니다.
- 기본 프로그램 버튼과 세트 복사/삭제 버튼의 크기, 테두리, 배치를 통일했습니다.
- 날짜별 기록 리스트를 날짜 헤더와 4개 지표 칸이 있는 카드로 다시 구성했습니다.

## v1.25.3 - 2026-05-26

- 운동 입력 폼의 운동명/장비/세트 행/복사/삭제 버튼 배치를 다시 잡아 입력칸과 버튼이 겹치지 않도록 보정했습니다.
- 기록 검색 결과를 운동명, 날짜/장소/장비, 중량/횟수 값 영역으로 분리해 리스트가 단순 텍스트처럼 보이지 않도록 재구성했습니다.
- 오늘 기록 수정 행의 버튼 영역을 grid 기반으로 보정해 모바일에서도 액션 버튼이 밀리지 않게 했습니다.

## v1.25.2 - 2026-05-26

- 기록/분석/식단/더보기 전반의 리스트 행을 카드형 박스 UI로 보강해 텍스트만 나열되는 느낌을 줄였습니다.
- 운동 기록 행, 세트/식단 상세 행, 식단 일자별 항목, 월간 식단 주차 카드, 누락/템플릿 카드를 공통 톤으로 맞췄습니다.
- 모바일에서 목록 행의 값/버튼이 억지로 한 줄에 붙지 않도록 세로 배치와 줄바꿈 처리를 보강했습니다.

## v1.25.1 - 2026-05-26

- 기록 점검/QA 리포트 신규 UI의 selector 범위를 전용 클래스 기반으로 좁혀 다른 표/목록 레이아웃에 영향이 가지 않도록 보정했습니다.
- 공통 표, 상세 목록, 요약 카드의 모바일 폭/줄바꿈/스크롤 처리를 다시 잡아 화면이 흐트러지지 않게 했습니다.
- PWA 캐시와 manifest 버전을 `v1.25.1`로 갱신했습니다.

## v1.25.0 - 2026-05-26

- 2.0 준비 상태 리포트를 QA 화면에 추가해 실사용 데이터, 연간 QA, 개인 잠금, 백업, 소스 분산 상태를 한 번에 점검하도록 했습니다.
- `services/release_readiness.py`를 추가해 릴리즈 준비도 계산을 라우트/템플릿 밖으로 분리했습니다.
- 앱 버전, manifest, service worker 캐시를 `v1.25.0`으로 갱신했습니다.

## v1.24.0 - 2026-05-26

- 세트 입력의 RPE, 메모, 세트 타입을 `고급` 접힘 영역으로 옮겨 모바일 기본 입력 밀도를 낮췄습니다.
- 동적 세트 추가 JS와 초기 템플릿을 같은 구조로 맞춰 복사/삭제/자동 행 생성 흐름이 같은 UI를 쓰도록 정리했습니다.
- 세트 행 CSS를 모바일 기준으로 다시 맞춰 기본 입력과 고급 입력이 겹치지 않게 했습니다.

## v1.23.0 - 2026-05-26

- 기록 점검 화면에 이상 입력 후보를 추가해 과도한 중량, 반복수, 유산소 시간, RPE 값을 바로 찾을 수 있게 했습니다.
- 이상 후보는 오늘 운동 화면으로 연결해 배포 후 실제 사용 중 오타를 빠르게 수정할 수 있게 했습니다.

## v1.22.0 - 2026-05-26

- 운동명 중복 후보 탐지를 추가해 공백/기호 차이로 갈라진 운동명을 기록 점검에서 확인할 수 있게 했습니다.
- `services/data_cleanup.py`를 추가해 데이터 정리 후보 계산을 별도 서비스로 분리했습니다.

## v1.21.0 - 2026-05-26

- 기록 점검 화면을 분석 신뢰도 보조 도구로 확장해 누락일, 중복 운동명, 이상 세트를 같이 확인하도록 했습니다.
- 정리 후보 UI를 카드형 목록으로 구성해 긴 데이터에서도 모바일 스캔이 가능하게 했습니다.

## v1.20.0 - 2026-05-26

- 더보기/설정에서 확인하는 QA 흐름에 2.0 전환 전 점검 기준을 연결했습니다.
- 백업/잠금/연간 더미데이터 상태를 릴리즈 준비도에 포함해 배포 전 확인 항목을 명확히 했습니다.

## v1.19.0 - 2026-05-26

- 오늘 운동 세트 입력을 기본 필수값 중심으로 간소화하고, 반복 입력 시 화면 높이를 줄였습니다.
- Fold 계열 모바일에서 세트 행의 액션 버튼과 입력칸 정렬이 유지되도록 회귀 테스트 마커를 추가했습니다.

## v1.18.0 - 2026-05-26

- `routes/main.py`의 일간/주간/월간 분석 및 설정 화면 컨텍스트 생성을 `services/summary_context.py`, `services/settings_context.py`로 분리했습니다.
- 오늘 운동 룰셋 카드와 주간 룰셋 리포트를 각각 partial 템플릿으로 분리했습니다.
- 룰셋 전용 스타일을 `static/rules.css`로 분리하고 base layout 및 PWA 캐시에 반영했습니다.
- 전체 QA를 다시 수행하고 앱 버전/PWA 캐시를 `v1.18.0`으로 갱신했습니다.

## v1.17.0 - 2026-05-26

- 로컬 운동 지식 룰셋을 추가해 부위별 주간 권장 세트 범위, RPE 기반 강도 조절, 권장 휴식시간, 대체 운동 후보를 계산합니다.
- 오늘 운동 화면에 `운동 룰셋` 카드를 추가해 지금 부족/과다/적정 부위와 다음 운동 조정 방향을 보여줍니다.
- 주간 분석 화면에 `운동 지식 룰셋` 섹션을 추가해 실제 기록과 권장 범위를 비교합니다.
- 룰셋 계산은 `services/exercise_rules.py`로 분리하고 앱 버전/PWA 캐시를 `v1.17.0`으로 갱신했습니다.

## v1.16.0 - 2026-05-26

- 운동명 선택 시 이전 기록과 운동별 설정을 기준으로 부위, 장비, 세트수, 무게, 횟수를 자동으로 채우는 스마트 기본값을 추가했습니다.
- 오늘 운동 화면에 `다음 액션` 패널을 추가해 부족한 부위, 회복 확인, 식단 보강, 분석 확인을 바로 이동할 수 있게 했습니다.
- 설정 화면에 개인 사용 안전 상태를 추가해 관리자 잠금, 보기 전용 세션, 자동 백업 상태를 한 번에 확인하도록 했습니다.
- 오늘 화면 컨텍스트, 스마트 운동 기본값, 개인 코칭/안전 상태 로직을 별도 서비스 파일로 분리해 `routes/main.py`와 `app.py` 집중도를 낮췄습니다.
- `static/workout_entry.js`를 추가하고 PWA 캐시와 앱 버전을 `v1.16.0`으로 갱신했습니다.

## v1.15.0 - 2026-05-26

- 오늘 운동 화면을 `오늘 할 일` 중심으로 간소화해 다음 입력, 마지막 기록, 휴식 타이머, 핵심 추천을 한 영역에서 확인하도록 정리했습니다.
- 운동 입력 폼은 기본 접힘 상태로 바꾸고, 운동 추가/추천 적용/최근 운동 불러오기 시 자동으로 열리게 했습니다.
- 운동 장소 패널, 운동 도구, 오늘 운동 계획을 보조 영역으로 정리해 모바일 첫 화면 밀도를 낮췄습니다.
- PWA 캐시와 앱 버전을 `v1.15.0`으로 갱신했습니다.

## v1.14.0 - 2026-05-26

- 다음 세트 자동 제안 서비스를 추가해 최근 기록, RPE, readiness 기준의 중량/반복/세트 추천을 오늘 운동 입력에 표시했습니다.
- 운동별 과부하 분석 서비스를 추가하고 분석 화면에 상승/정체/회복 필요 상태 카드를 표시했습니다.
- 최근 7일 부위 밸런스 히트맵을 분석 화면에 추가했습니다.
- 플레이트/워밍업 계산기를 독립 도구 페이지로 추가하고 더보기의 헬스장 도구 섹션에 연결했습니다.
- 다음 세트 추천 적용 버튼과 모바일 입력 보조 UI를 추가했습니다.
- 전체 테스트와 JS 문법 검증을 통과했고, PWA 캐시와 앱 버전을 `v1.14.0`으로 갱신했습니다.

## v1.13.0 - 2026-05-26

- DB 테이블 생성, 인덱스, 컬럼 보정, 초기 bootstrap 흐름을 `health_tracker/database/schema.py`로 분리했습니다.
- 운동 세트 순서 변경과 운동 생성 helper를 `services/workout.py`로 이동했습니다.
- 운동 메모, 운동별 휴식시간, 즐겨찾기, 기본 장비/목표 설정 로직을 `services/exercise_settings.py`로 분리했습니다.
- `app.py`를 `3555`라인에서 `3200`라인까지 추가 경량화했습니다.
- 전체 테스트와 컴파일 검증을 통과했고, PWA 캐시와 앱 버전을 `v1.13.0`으로 갱신했습니다.

## v1.12.0 - 2026-05-26

- `app.py`의 추천/회복/코칭 로직을 `services/coaching.py`로 분리했습니다.
- 루틴 템플릿과 운동 계획 로직을 `services/routine.py`, `services/workout_plan.py`로 분리했습니다.
- 체성분/진행 사진 로직을 `services/body.py`로 분리했습니다.
- 식단 템플릿, 식단 복사, 자주 쓰는 식단 조합 로직을 `services/meal.py`로 이동했습니다.
- 전체 테스트와 컴파일 검증을 통과했고, PWA 캐시와 앱 버전을 `v1.12.0`으로 갱신했습니다.

## v1.11.0 - 2026-05-26

- `app.py`에 남아 있던 날짜, 칼로리, 샘플 데이터, 캘린더, 부위 분석, 기록 검색, 장비 분석, PR 조회 로직을 서비스 모듈로 분리했습니다.
- 오늘 운동의 운동 시간, 휴식 타이머, 장소 패널을 partial 템플릿으로 분리해 화면 소스를 작게 나눴습니다.
- 운동 시간/휴식 타이머 JS를 `static/timers.js`로 분리하고 PWA 캐시에 포함했습니다.
- 전체 단위 테스트, Python 컴파일, JS 문법 검사, 주요 화면 렌더링 검증을 기준으로 QA했습니다.
- PWA 캐시와 앱 버전을 `v1.11.0`으로 갱신했습니다.

## v1.10.0 - 2026-05-26

- 앱 기본값 설정 서비스를 추가해 휴식 타이머, 기본 세트 수, 입력 placeholder, 세트 타입, 기본 목표값을 중앙 관리하도록 구조화했습니다.
- 설정 화면에 앱 기본값 관리 섹션을 추가해 주요 기본값을 코드 수정 없이 변경할 수 있게 했습니다.
- 오늘 운동 입력과 JS 세트 행 생성이 설정값을 공유하도록 바꿔 템플릿/JS의 세트 타입 및 휴식 preset 하드코딩을 줄였습니다.
- 일간 기록/기록 점검의 분석 기간 옵션과 주요 목록 기본 페이지 크기도 앱 기본값 설정을 따르도록 변경했습니다.
- PWA 캐시와 앱 버전을 `v1.10.0`으로 갱신했습니다.

## v1.9.8 - 2026-05-26

- 운동 모드에서 휴식 타이머가 운동 시간 바로 아래에 보이도록 모바일 표시 순서를 수정했습니다.
- 세트 저장 후 휴식 타이머가 자동으로 90초 시작되던 동작을 제거하고, 사용자가 타이머 버튼을 누를 때만 시작되도록 변경했습니다.
- PWA 캐시와 앱 버전을 `v1.9.8`로 갱신했습니다.

## v1.9.7 - 2026-05-26

- 오늘 운동의 운동 장소 카드에서 장비 카테고리와 최근 운동 목록이 붙어 보이지 않도록 간격과 구분선을 정리했습니다.
- 장소 안내 문구를 현재 동작에 맞게 운동명 후보는 장소 기준, 장비 카테고리는 전체 기본값 사용으로 수정했습니다.
- PWA 캐시와 앱 버전을 `v1.9.7`로 갱신했습니다.

## v1.9.6 - 2026-05-26

- 오늘 운동 카드에서 같은 운동의 세트명을 한 번에 바꾸는 운동명 일괄 수정 기능을 추가했습니다.
- 운동별 기본 장비 설정값을 오늘 운동 카드와 빠른 운동 선택 버튼에 함께 표시했습니다.
- 세트 수정 폼이 열릴 때 입력칸이 밀리지 않도록 편집 그리드 폭을 정리했습니다.
- PWA 캐시와 앱 버전을 `v1.9.6`으로 갱신했습니다.

## v1.9.5 - 2026-05-26

- 운동 시간 카드 바로 아래에 휴식 타이머를 배치했습니다.
- 장비 카테고리 선택은 장소와 무관하게 기본 카테고리 전체가 항상 보이도록 수정했습니다.
- 장소별 제한은 운동명 후보에만 적용되도록 장비 안내 문구를 정리했습니다.
- PWA 캐시와 앱 버전을 `v1.9.5`로 갱신했습니다.

## v1.9.4 - 2026-05-26

- 오늘 운동 입력의 운동명 후보, 최근 세트, 운동 통계, 즐겨찾기, 루틴 목록을 선택한 운동 장소 기준으로 제한했습니다.
- 장소가 다른 운동명은 선택한 장소의 운동 입력 후보에 섞이지 않도록 했습니다.
- 루틴 저장 시 원본 운동 세션의 장소를 함께 저장하도록 했습니다.
- PWA 캐시와 앱 버전을 `v1.9.4`로 갱신했습니다.

## v1.9.3 - 2026-05-26

- 장비 목록이 운동명이나 장비명으로 오염되지 않도록 장비 값을 카테고리로 정규화했습니다.
- 장소 장비 관리 입력을 자유 텍스트에서 `바벨`, `덤벨`, `머신`, `케이블`, `프리웨이트`, `맨몸`, `유산소기구` 카테고리 선택 방식으로 변경했습니다.
- 기존에 저장된 `스미스 머신`, `런닝머신` 같은 세부 장비명은 각각 `머신`, `유산소기구`로 정리되도록 했습니다.
- PWA 캐시와 앱 버전을 `v1.9.3`으로 갱신했습니다.

## v1.9.2 - 2026-05-26

- 기본 장비 목록에 `프리웨이트`를 추가했습니다.
- 오늘 운동 입력, 운동 설정, 세트 수정, 기록 검색 fallback 장비 목록에서 같은 기본 장비 구성을 사용하도록 반영했습니다.
- 장소별 장비 목록이 이미 있는 경우에도 `프리웨이트`는 공통 후보로 표시되게 했습니다.
- QA 더미데이터 장비 후보에도 `프리웨이트`를 추가했습니다.
- PWA 캐시와 앱 버전을 `v1.9.2`로 갱신했습니다.

## v1.9.1 - 2026-05-26

- 오늘 운동의 장비 선택 목록을 장소별 자동 수집 장비 우선으로 변경했습니다.
- 선택한 장소에 장비 기록이 있으면 기본 장비 전체 목록을 섞지 않고 해당 장소 장비만 표시합니다.
- 장소 장비가 아직 없을 때만 기본 장비 목록을 fallback으로 표시합니다.
- PWA 캐시와 앱 버전을 `v1.9.1`로 갱신했습니다.

## v1.9.0 - 2026-05-26

- 더보기 화면을 운동 관리, 분석과 기록, 식단과 데이터 섹션으로 재구성했습니다.
- `데이터 센터`, `장소 인사이트`, `실행 인사이트` 화면을 추가했습니다.
- 오늘 운동 화면에서 현재 장소 기준으로 자주 한 운동을 바로 불러올 수 있게 했습니다.
- 장소별 운동/장비 사용 요약과 데이터 백업/내보내기 상태를 더보기에서 확인할 수 있게 했습니다.
- PWA 캐시와 앱 버전을 `v1.9.0`으로 갱신했습니다.

## v1.8.0 - 2026-05-26

- 더보기에 `기록 점검` 화면을 추가해 최근 운동, 식단, 회복, 체성분 기록 누락과 분석 신뢰도를 한 번에 확인할 수 있게 했습니다.
- 더보기에 `식단 템플릿` 화면을 추가해 저장된 템플릿 적용, 삭제, 최근 식단 기반 템플릿 저장을 관리할 수 있게 했습니다.
- 더보기 카드와 PWA 캐시에 새 관리 화면을 추가했습니다.
- PWA 캐시와 앱 버전을 `v1.8.0`으로 갱신했습니다.

## v1.7.6 - 2026-05-26

- 운동 입력 세트 빌더에서 무게와 횟수 입력칸의 세로 기준선이 어긋나 보이던 문제를 수정했습니다.
- 무게 단위 선택과 kg 변환 미리보기 줄 때문에 생기던 높이 차이를 세트 입력 전용 그리드로 보정했습니다.
- PWA 캐시와 앱 버전을 `v1.7.6`으로 갱신했습니다.

## v1.7.5 - 2026-05-26

- 운동 장소 관리에서 기록이 없는 잘못 입력한 장소를 완전 삭제할 수 있도록 `삭제` 버튼을 추가했습니다.
- 기록이 연결된 장소는 기존 기록 보호를 위해 완전 삭제 대신 비활성화만 가능하게 유지했습니다.
- PWA 캐시와 앱 버전을 `v1.7.5`로 갱신했습니다.

## v1.7.4 - 2026-05-26

- 운동 장소 관리의 새 장소 추가 폼에서 입력칸이 겹쳐 보이던 문제를 수정했습니다.
- 장소 추가 체크박스가 과도하게 크게 보이지 않도록 화면 전용 크기를 적용했습니다.
- PWA 캐시와 앱 버전을 `v1.7.4`로 갱신했습니다.

## v1.7.3 - 2026-05-26

- 운동 장소 관리 화면을 다시 단순화해 기본 화면에는 장소 요약과 장비 칩만 보이도록 정리했습니다.
- 장소 수정과 장비 추가/제외 기능은 카드 안의 접힘 관리 영역으로 이동했습니다.
- 모바일에서 장소 카드 버튼, 새 장소 추가 폼, 장비 목록이 한 줄에 과밀하게 붙지 않도록 보정했습니다.
- PWA 캐시와 앱 버전을 `v1.7.3`으로 갱신했습니다.

## v1.7.2 - 2026-05-26

- 운동 장소 관리 화면을 상단 요약, 새 장소 추가, 장소별 수정/장비 관리 영역으로 재정리했습니다.
- 모바일에서 장소 카드 버튼과 입력칸이 좁게 겹치지 않도록 반응형 그리드와 버튼 폭을 보정했습니다.
- PWA 캐시와 앱 버전을 `v1.7.2`로 갱신했습니다.

## v1.7.1 - 2026-05-26

- 기존 로컬 DB에 `workout_sessions.location_id` 컬럼이 없을 때 장소 인덱스를 먼저 만들면서 `/more`가 500 오류로 열리지 않던 마이그레이션 순서 문제를 수정했습니다.
- 기존 DB 마이그레이션 회귀 테스트를 추가했습니다.
- PWA 캐시와 앱 버전을 `v1.7.1`로 갱신했습니다.

## v1.7.0 - 2026-05-26

- 헬스장/운동 장소 관리 기능을 추가했습니다.
- 기존 운동 기록은 기본 헬스장으로 자동 연결하고, 새 운동 저장 시 선택한 장소와 장비를 함께 저장합니다.
- 장소별 장비 목록을 오늘 운동 입력 화면에서 우선 표시하고, 운동 저장 시 해당 장소 장비 목록에 자동 반영합니다.
- 더보기의 운동 장소 관리 화면에서 장소 추가, 기본 장소 지정, 비활성화, 장소별 장비 추가/제외를 할 수 있게 했습니다.
- 기록 검색에 장소 필터와 장소명을 추가해 헬스장별 기록 확인이 가능하게 했습니다.
- PWA 캐시와 앱 버전을 `v1.7.0`으로 갱신했습니다.

## v1.6.8 - 2026-05-26

- 오늘 컨디션 카드에서 퍼센트 숫자가 `%`보다 작게 보이던 문제를 수정했습니다.
- PWA 캐시와 앱 버전을 `v1.6.8`로 갱신했습니다.

## v1.6.7 - 2026-05-26

- 오늘 운동 화면의 운동 진행 카드에서 `75초 휴식` 같은 추천 휴식 문구를 제거하고 `타이머 시작` 버튼으로 정리했습니다.
- 오늘 컨디션 카드의 퍼센트와 `/20` 점수 영역 폭과 숫자 정렬을 보정했습니다.
- PWA 캐시와 앱 버전을 `v1.6.7`로 갱신했습니다.

## v1.6.6 - 2026-05-26

- 기록 탭 부위별 분석의 전체 보기에서 특정 부위만 보이던 문제를 완화하기 위해 일간 부위 분석 조회 범위를 넓혔습니다.
- 부위 필터 링크에 섹션 앵커를 추가해 클릭 후 화면이 맨 위로 튀지 않고 부위별 분석 영역에 머물게 했습니다.
- 기록 페이징과 부위별 분석 섹션 사이 간격을 늘렸습니다.
- PWA 캐시와 앱 버전을 `v1.6.6`으로 갱신했습니다.

## v1.6.5 - 2026-05-26

- 부위별 분석 카테고리 필터를 기록 탭의 일간 기록 화면에만 적용되도록 재정리했습니다.
- 기록 화면의 부위 필터와 카드 영역 사이 여백을 조정해 UI가 붙어 보이는 문제를 완화했습니다.
- PWA 캐시와 앱 버전을 `v1.6.5`로 갱신했습니다.

## v1.6.4 - 2026-05-26

- 일간/주간/월간 분석의 부위별 분석 영역에 부위 카테고리 필터를 추가했습니다.
- 선택한 부위만 표시되도록 `part` 쿼리 필터를 적용하고, 필터 변경 시 페이지 번호는 초기화되게 했습니다.
- PWA 캐시와 앱 버전을 `v1.6.4`로 갱신했습니다.

## v1.6.3 - 2026-05-26

- 배포 서버의 UTC 시간대 영향으로 오늘 화면이 한국 시간보다 하루 늦게 잡힐 수 있던 문제를 수정했습니다.
- 앱의 오늘 날짜 계산을 `APP_TIMEZONE` 환경변수 기준으로 바꾸고 기본값을 `Asia/Seoul`로 설정했습니다.
- 날짜가 박히는 첫 화면(`/`, `/?mode=workout`, `/?mode=meal`)을 PWA precache에서 제외해 배포 후 이전 날짜 화면이 남을 가능성을 줄였습니다.
- PWA 캐시와 앱 버전을 `v1.6.3`으로 갱신했습니다.

## v1.6.2 - 2026-05-26

- 설정 비밀번호 PBKDF2 해시 생성/검증을 `health_tracker/security.py`로 옮겨 보안 책임을 한 모듈로 모았습니다.
- 비밀번호 해시 포맷과 오입력 거부를 확인하는 회귀 테스트를 추가했습니다.
- PWA 캐시와 앱 버전을 `v1.6.2`로 갱신했습니다.

## v1.6.1 - 2026-05-26

- CSRF 토큰과 관리자 보호 라우트 기준을 `health_tracker/security.py`로 분리해 `app.py`의 보안 공통 로직 중복을 줄였습니다.
- 전체 소스 점검 과정에서 PWA 캐시와 앱 버전을 `v1.6.1`로 갱신했습니다.

## v1.6.0 - 2026-05-26

- 방문자는 기록/분석을 볼 수만 있고, 수정/삭제/입력/export/import는 관리자만 가능하도록 읽기 전용 모드를 추가했습니다.
- 설정 비밀번호를 관리자 잠금으로 확장하고 민감 GET/export/API와 모든 POST 요청을 관리자 세션으로 보호했습니다.
- 모든 POST 요청에 CSRF 토큰 검증을 추가하고, 일반 form/JSON 운동 시간 저장 요청에 토큰을 붙이도록 했습니다.
- 방문자 화면에서는 작성/수정/삭제 UI를 숨기고 상단에 보기/관리자 상태를 표시합니다.
- PWA 캐시와 앱 버전을 `v1.6.0`으로 갱신했습니다.

## v1.5.3 - 2026-05-26

- 운동 입력 세트 행을 상단 액션형 카드 구조로 다시 정리했습니다.
- 무게/횟수 입력칸과 복사/X 버튼이 서로 밀리는 문제를 수정했습니다.
- X 삭제 버튼을 명확한 버튼 형태로 보이게 조정했습니다.
- PWA 캐시와 앱 버전을 `v1.5.3`으로 갱신했습니다.

## v1.5.2 - 2026-05-26

- 운동 입력 세트 빌더의 행 레이아웃이 깨지는 문제를 수정했습니다.
- 세트 번호, 무게/횟수 입력, 복사/삭제 버튼을 카드형 그리드로 재배치했습니다.
- 모바일 화면에서 세트 입력 필드가 한 열로 안정적으로 접히도록 조정했습니다.
- PWA 캐시와 앱 버전을 `v1.5.2`로 갱신했습니다.

## v1.5.1 - 2026-05-26

- 기록/분석 목록 페이징 기본 표시 개수를 모바일 기준에 맞춰 20개에서 10개로 낮췄습니다.
- 사용자가 직접 20개, 50개로 바꾸는 선택지는 유지했습니다.
- PWA 캐시와 앱 버전을 `v1.5.1`로 갱신했습니다.

## v1.5.0 - 2026-05-26

- 운동 입력에 `kg/lb` 단위 선택을 추가하고, `lb` 입력값은 저장 시 kg로 자동 변환되게 했습니다.
- 모바일 운동 입력을 여러 세트를 한 번에 만드는 세트 빌더 중심으로 개선했습니다.
- 3/4/5세트 프리셋, 같은 무게 채우기, 단계별 증량 채우기, kg 저장 미리보기를 추가했습니다.
- 세트 빌더 UI와 LB 변환 저장 테스트를 추가했습니다.
- PWA 캐시와 앱 버전을 `v1.5.0`으로 갱신했습니다.

## v1.4.0 - 2026-05-26

- 오늘 화면 첫 영역을 대시보드형 히어로로 바꾸고 핵심 액션을 명확히 했습니다.
- 기록 검색 결과를 수치 중심 카드형 리스트로 개선했습니다.
- 더보기 화면을 보조 도구 대시보드 톤으로 정리했습니다.
- 공통 카드/버튼/배경/빈 상태 스타일을 고도화했습니다.
- PWA 캐시와 앱 버전을 `v1.4.0`으로 갱신했습니다.

## v1.3.0 - 2026-05-26

- 기록 검색, PR 분석, 장비 분석 상단 UI를 대시보드형으로 정리했습니다.
- 기록/PR/장비 화면에 분석 화면 간 이동 액션을 추가했습니다.
- PWA 캐시와 앱 버전을 `v1.3.0`으로 갱신했습니다.

## v1.2.0 - 2026-05-26

- 주간/월간 분석 상단 UI를 연간 리포트와 같은 대시보드 헤드, 액션 버튼, 메트릭 카드 구조로 정리했습니다.
- 분석 화면의 주요 이동 경로를 PR, 운동별, 장비별, 연간 화면과 바로 연결했습니다.
- PWA 캐시와 앱 버전을 `v1.2.0`으로 갱신했습니다.

## v1.1.0 - 2026-05-26

- 오늘 화면의 `기록 상태`를 `분석 신뢰도`로 재정의하고 원형 점수 링, 근거 미니 바, 부족 항목 액션을 추가했습니다.
- 분석 신뢰도 계산을 `health_tracker/services/data_quality.py`로 분리했습니다.
- QA 더미데이터에 체성분 변화와 회복 패턴을 추가해 분석 화면 검증 기반을 강화했습니다.
- PWA 캐시와 앱 버전을 `v1.1.0`으로 갱신했습니다.

## v1.0.0 - 2026-05-26

- Galaxy Z Fold7 기준 모바일 UI 정리
- 운동 입력 빠른 선택 탭, 컨디션 코칭 카드, 기록 상태 카드 추가
- 기록/분석 페이징, 연도별 분석, QA 더미데이터, QA 리포트 추가
- 설정 비밀번호 잠금/재설정, 더보기/기록/분석 메뉴 중복 정리
- 전체 QA 회귀 테스트와 PWA 캐시 기준 정비
